[package]
name = "aiform"
version = "0.1.0"
edition = "2021"

[lib]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
async-openai = "0.20"
aiform-macros = { path = "aiform-macros" }
tokio = { version = "1.0", features = ["full"] }
